#Git Commands
git --version
git config --list
git config --global user.name "username"
git config --global user.email "email"
git init:- Used for initializing a git 
git add --all/./filename :- Used for adding a new file to git
git commit -m "message":-Used for Adding to Git Local
git push -u origin <branch-name>: Used for trsnferring data to git hub 
git pull :- this is used to pull
git status :- used for listing the untracked file
git branch :- used to list the local branch